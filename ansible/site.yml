---
- name: Baseline environment setup
  hosts: local
  become: true

  tasks:
    - name: Ensure baseline packages are installed
      dnf:
        name:
          - git
          - curl
          - wget
        state: present
